import RedirectPage from '@/components/RedirectPage';

export default function UIScreenDesignRedirect() {
  return (
    <RedirectPage
      destination="/services#creative"
      title="Service Moved"
      message="UI Screen Design services are now part of our Creative services section."
    />
  );
}